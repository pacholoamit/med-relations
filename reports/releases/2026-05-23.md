# Release Notes — 2026-05-23

**Period:** 2026-05-18 → 2026-05-23

29 PR(s) merged to master across 3 repo(s) — 20 hotfix(es), 9 planned release(s).

---

## Hotfixes

### [HOTFIX] Campaign Approval for PCL

Fixed the campaign approval flow for PCL (Proposal/Contract Line) accounts so approvals process correctly in production.

### HOTFIX Remove Statara Partitions job

Removed the Statara partitions background job from the microservices pipeline as an emergency stability fix.

### HOTFIX Add new internal service env to production

Added missing environment variables to the internal service's production configuration to restore correct runtime behavior.

### [HOTFIX] Add type field to all campaign order report queries for Demo Campaign

Added the `type` field to all campaign order report queries so Demo Campaign data correctly reflects its campaign type across report views.

### [HOTFIX] Fix Demo Campaign

Patched a production issue affecting the Demo Campaign in the GQL service to restore correct data display.

### [HOTFIX] Remove Audience Type dropdown from Demograph reach panel

Removed the non-functional Audience Type dropdown from the Demograph reach panel that was creating UI inconsistency; the reach gauge now always displays Devices Reached, while both Devices and Individuals tiles remain visible side-by-side.

### [HOTFIX] Removed Salesforce Rate-Limit

Removed the Salesforce API rate-limit guard from the GQL service that was incorrectly throttling production opportunity creation requests.

### [HOTFIX] Salesforce Additional Fields in PCL

Extended the GQL Salesforce schema to forward optional PCL fields (`LeadSource`, `Serviced_Account__c`, `RecordTypeId`, `Deal_Signatory__c`, `Cpm__c`) through to Salesforce on opportunity and line-item creation.

### Revert "[HOTFIX] Salesforce Additional Fields in PCL"

Reverted the Salesforce Additional Fields PR due to production issues encountered after merging; a corrected re-application followed shortly after.

### HOTFIX Fix reprocess button not process on cluster

Fixed the campaign reprocess button so it correctly dispatches processing jobs when running on the production cluster.

### HOTFIX For reprocess button remove the prisma1 query

Streamlined the campaign reprocess button's job queue by removing the legacy `getLogs` Prisma 1 job, so only display, events, and S3 report jobs are enqueued on reprocess.

### [HOTFIX] Added Salesforce Fields and CPM

Rewired the Create Salesforce Opportunity flow to correctly resolve CPM pricing for Channel-Partner deals (using `contractCPM` as the cost basis), added a Months input for line item quantities, and added a Skip PandaDoc toggle to the Campaign Order Queue approval modal.

### [HOTFIX] Salesforce Additional Fields in PCL (re-apply + new fields)

Re-applied the reverted Salesforce schema extensions and added additional PCL fields (`Contract_Type__c`, `Number_of_months__c`, `ServiceDate`, `Service_End_Datee__c`) needed for the full PCL opportunity and line-item flow.

### [HOTFIX] Removed Console Log

Removed an inadvertent console log statement left in production code.

### [HOTFIX] Fix page flickers after clicking task link

Added defensive coding to prevent the task detail page from flickering when navigating via a task link or hard-refreshing a task detail URL.

### hotfix(data-ingestion-factory): prevent 401 on first load of Widget Dev page

Fixed silent 401 errors on the Widget Development page by gating all widget manager queries on token presence, so the Connect button and widget data load correctly on first visit rather than firing unauthenticated requests on mount.

### hotfix(data-ingestion-factory): unblock Ingest button for fixed-template adapters

Unblocked the Ingest button for fixed-template adapters (Jane, Shopify, Dutchie) on both the Configure Datasets and Datasets List pages, and fixed the backend to correctly preserve incremental sync state when ingestion is manually triggered.

### fix(maps): pin Google Maps API to v3.64 to restore HeatmapLayer

Pinned the shared Google Maps loader to API version 3.64 after Google removed `HeatmapLayer` in v3.65 (rolled out via the `weekly` channel), restoring all five heatmap pages that were white-screening in production across OrgConfig Targeting, Audiences, Tracking, SEO, and the shared HeatMap component.

### hotfix(demograph): fix publish pipeline - address out-of-memory, resume from remaining chunk to be upload

Hardened the Statara/Nexxen demograph publish pipeline to survive out-of-memory conditions by adding chunk-level resume capability, SFTP connection pooling, cleanup sweeps, and graceful cancellation support following a multi-day incident response.

### [HOTFIX] Restructure task management route to avoid flickering

Fixed the entire Task Management section's persistent flicker-on-refresh by stabilizing the `/tasks` route reference with `useCallback`, preventing React Router from remounting the Tasks component on every render cycle; also standardized the loading spinner across task detail and project detail views.

---

## Planned Releases

### feat(data-factory): add sync modes across data-factory v1 + v2 UIs with adapter-managed defaults

Added sync mode configuration to both data-factory v1 and v2 UIs, allowing each adapter to declare its default sync mode while letting users override the mode per dataset.

### [MD-11302] Campaign Requestor Enhancements and Line Item Management

Significantly expanded the Campaign Requestor with bulk line item management (ad group preview, click-through URL config), AI-powered column mapping for line item imports, bulk targeting and audience tabs, Campaign Deal IDs bulk-apply, Nexxen sync and individual save, SSP defaults, and delivery filter improvements.

### [MS-11302] Nexxen Integration Fixes and Data Factory Cleanup

Reverted unsupported `includeSegmentId` changes from the Nexxen integration, fixed insertion-order DTO types, simplified data-factory sync mode configuration, and added a `repair-statara-partitions` job template.

### ez nexxenCredentialForwardFix

Stopped the Nexxen adapter from forwarding internal credentials (`clientId`, `clientSecret`, `nexxenApiUrl`) to the external service, eliminating a credential-leakage risk in the data-factory pipeline.

### Multiple Transactions Fix

Fixed the campaign transactions table showing duplicate entries by scoping the query to campaign-specific transactions instead of retrieving all transactions including the global by-campaign set.

### fix(paid-search): guard xAxis access when Google data is empty

Added null and undefined guards around `overallData.byDate` and `overallLocations.byDate` access in the Paid Search module, preventing white-screen crashes when a Google Ads account has no paid-search data.

### Fix Paid Search Missing Dates

Resolved five compounding bugs that had frozen paid-search reporting at 2026-05-07: stale key conflicts from orphaned `PaidSearchDateUnit` rows, a 14-year date range caused by Google's `2037-12-30` no-end-date sentinel, a null `searchStream` response crash, a chunk slice off-by-one dropping one record per thousand, and a `concat(false)` call that injected junk rows into the consolidated byDate set.

### fix(data-factory): stop ingestion scheduler infinite loop

Fixed two interacting defects in the data-factory ingestion scheduler that caused it to re-arm jobs every ~6ms, hammering external DSP APIs with real credentials — widened the job-state filter to include `waiting`, `active`, and `paused` states, and added a minimum full-interval clamp when the calculated next-run delay is zero.

### Fix paid-search-prisma6 crashes: empty campaigns and Decimal aggregation

Fixed two production crashes in the Prisma 6 paid-search job: a cross-org campaign lookup that threw on `undefined.id`, and Decimal string concatenation in the aggregated channel total calculation that produced malformed values rejected by the database.

---
*Generated by ReleaseNotesAgent*
